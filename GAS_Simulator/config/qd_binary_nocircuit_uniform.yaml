experiment_name: qd_binary_nocircuit_uniform

problem:
  n_key: 4
  n_val: 5
  objective_function: x0 + x1 + x2 + x3
  variable_type: binary

algorithm:
  method: qd
  initial_state: uniform
  # dicke_state等を使うなら state_prep_params を追加する
  # state_prep_params:
  #   k: 3
  # initial_state: dicke
  # state_prep_params:
    # k: 1

simulation:
  backend: statevector
  # convergence_backend を書かなければ nocircuit が選ばれる想定
  convergence_backend: nocircuit
  qd_nocircuit_mode: fejer
  max_iterations: 20
  seed: 0

execution:
  num_trials: 10

circuit_assets:
  build: true

# 回路コスト評価をトランスパイル後でやりたい場合は、あなたの実装に合わせて追加
# circuit_evaluation:
#   do_transpile: true
#   optimization_level: 1
#   basis_gates: ["cx", "rz", "sx", "x"]
